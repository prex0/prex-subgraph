{
  "name": "@blockerage/subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "test": "graph test",
    "build": "graph build",
    "fmt:fix": "prettier --write src/**/*",
    "prepare:arbitrum": "mustache config/arbitrum.json subgraph.template.yaml > subgraph.yaml",
    "deploy:studio:arbitrum": "graph deploy --studio predyv6arbitrum"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.55.0",
    "@graphprotocol/graph-ts": "0.29.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.0",
    "mustache": "^4.0.1",
    "prettier": "^2.5.1"
  }
}